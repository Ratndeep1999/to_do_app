import 'package:flutter/material.dart';

class OnboardingScreensWidget extends StatelessWidget {
  const OnboardingScreensWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
